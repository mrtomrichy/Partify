<resources>

    <!-- Base application theme. -->
    <style name="Partify" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/Partify.ActionBar</item>
        <item name="android:windowBackground">@color/spotify_grey</item>
        <item name="android:buttonStyle">@style/Partify.Button</item>
    </style>

    <!-- ActionBar styles -->
    <style name="Partify.ActionBar"
           parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@android:color/black</item>
        <item name="android:titleTextStyle">@style/Partify.ActionBar.Title</item>
        <item name="android:subtitleTextStyle">@style/Partify.ActionBar.Subtitle</item>
    </style>

    <style name="Partify.ActionBar.Title" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Partify.ActionBar.Subtitle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Subtitle">
        <item name="android:textColor">@color/spotify_green</item>
    </style>

    <style name="Partify.Button" parent="@android:style/Widget.Holo.Light.Button">
        <item name="android:background">@drawable/button_selector</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:layout_margin">20dp</item>
        <item name="android:minWidth">220dp</item>
    </style>

</resources>
